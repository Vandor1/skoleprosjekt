package com.prosjekt.prosjekt.appuser;

/**
 * Enum AppUserRole, or 'Authorities'. Roles which can be given an AppUser which can be used to give access to certain
 * pages and actions in WebSecurityConfig.class.
 */
public enum AppUserRole {
    USER,
    ADMIN
}
